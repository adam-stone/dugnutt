## Dugnutt Frontend

The Dugnutt frontend is a simple Svelte app that gets built and can be served
statically by the FastAPI backend, or hosted elsewhere.

## Build Process

```
npm install
npm run build
```

